<div class="container mb-5">
  <div class="row mt-5">
<div class="col-md-12 text-center">
  <h2>本当に退会しますか？</h2>
  <p class="mt-5">退会すると、会員登録情報や</br>
  これまでの購入履歴が閲覧できなくなります。</br>
    退会する場合は、『退会する』をクリックしてください。</p>
    <% if customer_signed_in? && @customer.id == current_customer.id %>
      <%= link_to "退会しない", customers_path, class: "btn btn-primary mr-3" %>
        <%= link_to "退会する", customers_withdraw_path(current_customer.id), method: :patch, class: "btn btn-danger" %>
    <% end %>
    </div>
  </div>
</div>