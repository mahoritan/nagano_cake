<div class="container">
  <div class="row mt-5 ml-5">
    <h4 class="bg-light text-dark">　ジャンル編集　</h4>
  </div>

  <div class="row">
    <div class="col-md-12">
    <%= form_with model:@genre, url: admin_genre_path(@genre.id) ,local:true do |f| %>
  <h6>ジャンル名</h6>
  <%= f.text_field :name , class: 'col-md-4' %>
  <%= f.submit '変更を保存' %>
  </div>
<% end %>
  </div>
  </div>